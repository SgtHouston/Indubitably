<!DOCTYPE html>
    <html lang="en">

    <%-include('partials/head') %>

        <body>
            <%-include('partials/nav') %>
                <h1>
                    <%= title %>
                </h1>
                <% jobs.forEach((job)=> { %>
                    <h2>
                        <%=job.title %>
                    </h2>
                    <a href="/jobs/<%=job.id %>">Get details</a>
                    <% })%>
        </body>

    </html>